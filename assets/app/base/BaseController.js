'use strict';

cbApp.controller('BaseController', ['$scope', '$log', '$state',
    function BaseController($scope, $log, $state) {
        self.Init = function () {
        };
        self.Init();
    }]
);